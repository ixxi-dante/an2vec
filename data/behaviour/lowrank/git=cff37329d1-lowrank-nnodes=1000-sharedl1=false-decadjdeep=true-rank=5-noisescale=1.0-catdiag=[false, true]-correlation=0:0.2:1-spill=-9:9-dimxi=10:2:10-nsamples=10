date; PATH=$PATH:/home/slerique/bin OMP_NUM_THREADS=1 JULIA_NUM_THREADS=1 parallel --sshloginfile parallel-slaves --sshdelay 0.1 --bar --header : --env PATH --env JULIA_NUM_THREADS --env OMP_NUM_THREADS 'cd /home/slerique/Code/nw2vec && if test $(( 10 + {spill} )) -gt {dimxi} || test $(( 10 - {spill} )) -gt {dimxi}; then false; fi && OUTFOLDER=data/behaviour/lowrank/git=cff37329d1-nnodes=1000-sharedl1=false-decadjdeep=true-rank={rank}-noisescale={noisescale}-catdiag={catdiag} && OUTPATH=$OUTFOLDER/correlation={correlation}-spill={spill}-dimxi={dimxi}-sample={sample}.npz && if test -e $OUTPATH; then echo "Already done."; false; fi && mkdir -p $OUTFOLDER && nice -n 5 julia julia/an2vec-generative.jl -l 100 -k 10 --p_in 0.25 --p_out 0.01 --correlation {correlation} --featuretype lowrank --flowrank_dim {rank} --flowrank_rank {rank} --flowrank_noisescale {noisescale} --flowrank_catdiag {catdiag} --dimxiadj $(( 10 + {spill} )) --dimxifeat $((10 - {spill} )) --overlap $(( 20 - {dimxi} )) --savehistory $OUTPATH --bias false --sharedl1 false --decadjdeep true' ::: catdiag true false ::: noisescale 1.0 ::: rank 5 ::: correlation $(seq 0 0.2 1) ::: spill $(seq -9 9) ::: dimxi $(seq 10 2 20) ::: sample $(seq 1 10)
